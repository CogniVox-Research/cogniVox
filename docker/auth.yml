services:
  auth_service:
    restart: "unless-stopped"
    build:
      context: ../
      dockerfile: ./services/auth/Dockerfile
    ports:
      - "8010:8010"
    volumes:
      - ../services/auth/config.toml:/app/config.toml:ro
      - ../services/auth/keys:/app/keys:ro
