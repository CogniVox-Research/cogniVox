services:
  llm_service:
    restart: "unless-stopped"
    build:
      context: ../
      dockerfile: ./services/llm/Dockerfile
    ports:
      - "8030:8030"
    env_file:
      - ../services/llm/.env
