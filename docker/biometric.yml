services:
  biometric_service:
    image: ghcr.io/cognivox-research/biometric_service:v0.0.1
    restart: "unless-stopped"
    ports:
      - "8011:8011"
    volumes:
      - ./config/biometric.toml:/app/services/biometric/config.toml:ro
