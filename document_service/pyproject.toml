[project]
name = "document-service"
version = "0.1.0"
description = ""
authors = [
    {name = "navindunimsara2001",email = "navidunimsara@gmail.com"}
]
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "fastapi[standard] (>=0.128.0,<0.129.0)",
    "uvicorn (>=0.40.0,<0.41.0)",
    "python-multipart (>=0.0.21,<0.0.22)",
    "pypdf (>=6.5.0,<7.0.0)"
]

[tool.poetry]
packages = [{include = "document_service", from = "src"}]

[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
