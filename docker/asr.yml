services:
  asr_service:
    image: ghcr.io/cognivox-research/asr_service:v0.0.1
    restart: "unless-stopped"
    ports:
      - "8001:8001"
    volumes:
      - ../services/asr/models:/app/models
      - ../services/asr/Rocket.toml:/app/Rocket.toml:z
