services:
  gateway:
    restart: "unless-stopped"
    ports:
      - "7000:80"

    depends_on:
      game_service:
        condition: service_healthy
      auth_service:
        condition: service_healthy
      document_service:
        condition: service_healthy

    build:
      context: ../
      dockerfile: ./services/gateway/Dockerfile
